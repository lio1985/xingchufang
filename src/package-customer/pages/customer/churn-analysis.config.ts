import type { PageConfig } from '@tarojs/taro';

const config: PageConfig = {
  navigationBarTitleText: '预警效果分析',
  navigationBarBackgroundColor: '#0f172a',
  navigationBarTextStyle: 'white',
  backgroundColor: '#0f172a'
};

export default config;
